{
  "name": "@tokenring-ai/websearch",
  "version": "0.2.0",
  "description": "Pluggable interface for searching the web and retrieving live page content.",
  "dependencies": {
    "@tokenring-ai/app": "workspace:*",
    "@tokenring-ai/agent": "workspace:*",
    "@tokenring-ai/chat": "workspace:*",
    "@tokenring-ai/utility": "workspace:*",
    "@tokenring-ai/scripting": "workspace:*",
    "zod": "^4.4.3"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "scripts": {
    "test": "bun test",
    "build": "tsc --noEmit",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  }
}